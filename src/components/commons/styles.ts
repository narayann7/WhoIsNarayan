type css = React.CSSProperties;
const center: css = {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

const left: css = {
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "center",
};

const right: css = {
  display: "flex",
  justifyContent: "flex-end",
  alignItems: "center",
};

export const styles = {
  center,
  left,
  right,
};
